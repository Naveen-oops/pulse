apiVersion: apps/v1
kind: Deployment
metadata:
  name: poll-service
  labels:
    app.kubernetes.io/name: poll-service
    app.kubernetes.io/part-of: pulse
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: poll-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: poll-service
        app.kubernetes.io/part-of: pulse
    spec:
      containers:
        - name: poll-service
          image: pulse/poll-service
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: PULSE_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: pulse-db
                  key: poll-service-url
            - name: PULSE_DB_SCHEMA
              value: polls
            - name: PULSE_PRESENTER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pulse-app
                  key: presenter-token
            - name: PULSE_API_PREFIX
              value: /api/polls
            - name: PULSE_CORS_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: pulse-config
                  key: cors-origins
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: poll-service
  labels:
    app.kubernetes.io/name: poll-service
    app.kubernetes.io/part-of: pulse
spec:
  selector:
    app.kubernetes.io/name: poll-service
  ports:
    - name: http
      port: 80
      targetPort: http
